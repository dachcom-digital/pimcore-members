{% extends '@Members/Form/form_wrapper.html.twig' %}

{% block form %}
    {{ form_start(form, { 'action': path('members_user_resetting_reset', {'token': token}), 'attr': {'novalidate': 'novalidate', 'class': 'members_user_resetting_reset' } }) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock form %}