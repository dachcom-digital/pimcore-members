{% extends '@Members/form/form_wrapper.html.twig' %}

{% block form %}
    {{ form_start(form, { 'action': path('members_user_resetting_send_email'), 'attr': {'novalidate': 'novalidate', 'class': 'members_user_resetting_request' } }) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock form %}